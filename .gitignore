/target
node_modules
dist
.claude/settings.local.json
tmp/
docs/roadmaps/
docs/plans/
.claude/settings.json
docs/handoff/
docs/reviews/
.agents/skills/shadcn/

# Harness install/runtime mirrors; canonical source is the catalog and the
# local source dir. Regenerated by `kendex refresh`.
/.agents/skills/
/.claude/skills/
/.codex/
/.cursor/
/.opencode/
/.pi/
# Per-checkout install lock (absolute paths and timestamps).
.kendex-lock.json
.env.local

# The linear skill's local cache
.cache/

# Filled-in local working copies of docs (backed up privately, never committed).
docs/*-local.md
