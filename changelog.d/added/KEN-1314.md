- `code-quality-load-check` hook (Claude Code): an edit inside a git work tree is refused until the session has loaded the `code-quality` skill; the work tree's own `tmp/` and paths outside one pass.
