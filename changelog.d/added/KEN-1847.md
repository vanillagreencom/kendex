- orch: `oversee launch` opens a fleet's first overseer and `oversee register` records a hand-opened one, both through the `overseer-host` adapter; `ORCH_TMUX_SESSION` drives lanes from outside tmux.
