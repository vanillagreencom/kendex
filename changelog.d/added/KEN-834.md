- growth-guards adds a `prose` lane, on by default: a date, an issue number or
  a past-state word fails in the markdown agents load. Scope:
  `GROWTH_GUARDS_PROSE_PATHS`; off: `GROWTH_GUARDS_CHECKS`.
