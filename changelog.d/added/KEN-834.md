- growth-guards adds a `prose` lane, on by default: a date, an issue number or
  a past-state word in `SKILL.md`, `workflows/*.md`, `AGENTS.md` or
  `CLAUDE.md` fails. Scope: `GROWTH_GUARDS_PROSE_PATHS`.
