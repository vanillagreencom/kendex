- growth-guards adds a `prose` lane, on by default: a date, an issue number or
  a past-state word fails in the markdown agents load — skills, workflows,
  agent definitions, `AGENTS.md`, `CLAUDE.md`.
