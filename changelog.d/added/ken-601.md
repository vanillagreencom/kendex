- `changelog-entries` judges `changelog.d/*/*.md` fragments and, over
  `CHANGELOG.md` by default, refuses a record with no `## [Unreleased]`
  heading; empty `GROWTH_GUARDS_CHANGELOG_RECORD` to opt out.
