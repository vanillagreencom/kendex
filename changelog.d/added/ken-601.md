- `changelog-entries` judges fragments (`changelog.d/*/*.md`, 200 characters
  each) and refuses hand-added `[Unreleased]` lines in `CHANGELOG.md`; empty
  `GROWTH_GUARDS_CHANGELOG_RECORD` to opt out.
