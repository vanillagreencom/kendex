- **Breaking:** `.kendex-lock.json` is committed and portable; per-machine fields move to `.cache/kendex/lock-local.json`. An older lock is refused: move it aside, run `kendex apply --record-existing`
