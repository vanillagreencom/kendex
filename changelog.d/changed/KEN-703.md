- Each skill declares the keys a consumer sets in its own
  `kendex.settings.toml.example`; keys only a maintainer touches stay in
  its docs. The repo-root example is gone; authoring: `docs/authoring`.
