- Each skill ships the keys it seeds in its own `kendex.settings.toml.example`;
  keys it documents without seeding stay in its own docs. The repo-root example
  is gone; authoring a skill's settings: `docs/authoring/settings.md`.
