- Each skill ships the keys it seeds in its own `kendex.settings.toml.example`;
  keys it only documents stay in its own docs. The repo-root example is gone;
  authoring: `docs/authoring/settings.md`.
