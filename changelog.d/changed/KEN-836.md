- Commits are judged for work markers on what they add: `todo-ban --staged` runs
  in the pre-commit chain, the index-wide scan is CI's, preflight's lane is gone,
  and a quoted marker is out of scope.
