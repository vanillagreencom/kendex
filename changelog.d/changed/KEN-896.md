- `kendex marketplace new --dir` refuses when the containing folder does not
  exist, naming it, instead of creating the chain. Importing refuses two
  selections that land in one place, naming both.
