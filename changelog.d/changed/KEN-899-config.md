- A command whose message, heredoc or comment names one of those words is
  refused too; the refusal names the word and the rewrite. Split the command,
  or pass the message with `git commit -F <file>`.
