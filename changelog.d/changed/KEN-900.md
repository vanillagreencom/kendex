- `kendex check` reports commit hooks in the growth-guards package's own words
  and exit code, asking it rather than reading the hook files, so it can no
  longer disagree with `kendex guard check`.
