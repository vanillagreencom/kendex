- `kendex check` relays growth-guards' own commit-hook verdict when it has one, and
  only where `.git/hooks` holds an install's helper. kendex reads no hook file; the
  stranded-shim report is gone.
