- `kendex check` prints the growth-guards package's own commit-hook verdict
  instead of reading the hook files, and only where `.git/hooks` holds an
  install's helper. The stranded-shim report is gone.
