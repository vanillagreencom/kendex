- **Breaking:** Claude, Pi and OpenCode withhold the question tool by
  `role: planner`, not by the name `planner`, so a rename changes no tool.
  Declare the role on a `planner` agent that has none.
