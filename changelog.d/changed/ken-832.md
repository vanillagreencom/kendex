- size-ratchet measures markdown in bytes, ships its own class list so a repo
  configures nothing, lowers a shrunk row itself on a commit it passes, and
  refuses a raised row on markdown and tests.
