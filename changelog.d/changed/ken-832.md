- **Breaking:** size-ratchet counts markdown in bytes, so existing markdown
  rows fail until `size-ratchet --update` rewrites them. The shipped class
  list drops this repo's ui and README overrides.
