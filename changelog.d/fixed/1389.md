- `kendex refresh` installs and records the Pi packages a scope declares whose install runs no `npm install`, after its one confirmation, so a fresh clone with no install record refreshes in one run.
