- A check and the writes that change what it reports now wait for each
  other, so a check answering mid-write no longer puts the old rows back.
  Every Updates control waits while any one of them runs.
