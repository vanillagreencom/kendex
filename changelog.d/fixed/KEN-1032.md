- A check and the writes that change what it reports now wait for each
  other, so a check answering while an update, a mute, a Follow flip or a
  version switch runs no longer puts the old rows back.
