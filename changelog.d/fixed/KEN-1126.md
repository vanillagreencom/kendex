- size-ratchet: adopting the byte-counted markdown class re-measures existing
  rows in one `--update`; files the new unit puts over the threshold become new
  offenders and need a row added by hand.
