- `kendex check --catalog` fails a `[bundles.<name>]` body with a key kendex
  does not read, dropping that set alone, and `kendex add --bundle` refuses a
  set whose members the catalog does not offer.
