- `kendex check --catalog` fails a `[bundles.<name>]` body with a key kendex
  does not read, `add --bundle` refuses a set the catalog offers no member of,
  and apply keeps what such a catalog installed.
