- A `[bundles.<name>]` body kendex will not read is now reported and that set
  alone dropped, and `kendex add --bundle` refuses a set the catalog offers no
  member of, instead of installing nothing.
