- Forking or renaming an agent onto a name that drops a built-in tool deny
  is refused, on every declared tool. Catalog settings carry; a fork needs
  one known revision across them, so refresh first.
