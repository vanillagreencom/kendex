- Forking or renaming an agent onto a name that drops a built-in tool deny is
  refused, on every tool it is declared for. Catalog settings are carried, and
  a fork across mismatched revisions refuses.
