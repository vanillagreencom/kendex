- Renaming or forking an agent onto a name that drops a built-in tool deny is
  refused, on every tool it is declared for. Catalog per-tool settings are
  carried, so a fork that widens nothing runs.
