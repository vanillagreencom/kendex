- The safety score counts a dangerous switch where a line would run it: a
  README or `case` arm naming one no longer scores, and one on a command
  line, quoted or in a Python or Rust file, still does.
