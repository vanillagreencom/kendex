- A catalog checks out the same bytes on every machine: its own
  `.gitattributes` no longer converts line endings or runs a smudge filter,
  and the mirror kendex clones carries no host git template.
