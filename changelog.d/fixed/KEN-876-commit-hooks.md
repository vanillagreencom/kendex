- `kendex check` no longer reports a repository's armed git hooks as
  unverifiable when it runs from a linked worktree, where the helper names the
  checkout that armed it rather than the one reading.
