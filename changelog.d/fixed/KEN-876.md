- `kendex verify` and `kendex check` now answer inside a git worktree, and
  answer clean: the lock copied in from the main checkout reads against the
  worktree it sits in, never the one it came from.
