- `kendex verify` and `kendex check` now answer inside a git worktree: the lock
  copied in from the main checkout reads against the worktree, so neither can
  name — or prune — the checkout it came from.
