- `kendex verify` and `kendex check` now answer inside a git worktree, and
  `kendex refresh` there works on the worktree: a lock copied in from the main
  checkout reads against the tree it sits in.
