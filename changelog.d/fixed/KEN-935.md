- Importing a skill or agent under a new name writes that name into the copy's
  own declaration instead of landing a package named something else, and
  refuses where the file carries no frontmatter.
