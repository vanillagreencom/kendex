- kendex reads only the format it writes: a lock or manifest from any other version is refused and left as written, in both directions. A pi `hooks/` or `hooks.json` beside a root is yours to delete.
