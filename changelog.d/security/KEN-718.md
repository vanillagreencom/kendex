- `kendex update` verifies the desktop app download against the signature the
  release publishes, and refuses a mismatch rather than installing it. The
  installed app and CLI are left as they were.
