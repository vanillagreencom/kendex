- The update card no longer offers `sudo kendex update` for a `kendex` command
  it cannot write. It points at the installer, which reaches no path off the
  machine but is not held to the release key.
