- The update card no longer offers `sudo kendex update` for a `kendex` command
  it cannot write: the installer instead, or the download page where there is
  none. Neither is held to the release key.
